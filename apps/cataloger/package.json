{
  "name": "@litomi/cataloger",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build --compile --minify --sourcemap src/index.ts --target=bun --outfile dist/cataloger",
    "start": "bun run start:local",
    "start:local": "bun --env-file=.env.local src/index.ts",
    "start:prod": "bun --env-file=.env.prod.runtime src/index.ts",
    "type": "tsc --noEmit",
    "docker:build": "docker build -f Dockerfile -t litomi-cataloger-local ../..",
    "docker:run": "docker run --rm --env-file .env.prod.runtime litomi-cataloger-local"
  },
  "dependencies": {
    "@litomi/catalog": "workspace:*",
    "@litomi/crawler": "workspace:*",
    "@litomi/db": "workspace:*",
    "@litomi/domain": "workspace:*",
    "dayjs": "^1.11.21",
    "drizzle-orm": "1.0.0-rc.4"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  }
}
