# Google Cloud Project Configuration
PROJECT_ID=your-project-id
REGION=asia-northeast1
JOB_NAME=catalog-ingest
ARTIFACT_REGISTRY_REPO=cloud-run-jobs
SERVICE_ACCOUNT_NAME=catalog-ingest-sa
JOB_SCHEDULE="0 2 * * *"

# Application Configuration
AIVEN_POSTGRES_URL=postgresql://user:password@host/database?sslmode=require
